# Part 1 – MLP from Scratch in NumPy

A multi-layer perceptron with hand-derived backpropagation, trained on MNIST using only NumPy.

## Results

_TBD: test accuracy, loss/accuracy curves._

## How it works

_TBD: forward pass, backward pass derivations (Linear, ReLU, softmax + cross-entropy), gradient checking._

## Run

```bash
python part1_numpy/train.py
pytest part1_numpy/tests
```
